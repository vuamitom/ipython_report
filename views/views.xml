<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="exper.list">
      <field name="name">exper list</field>
      <field name="model">exper.exper</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="exper.action_window">
      <field name="name">exper window</field>
      <field name="res_model">exper.exper</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="exper.action_server">
      <field name="name">exper server</field>
      <field name="model_id" ref="model_exper_exper"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="exper" id="exper.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="exper.menu_1" parent="exper.menu_root"/>
    <menuitem name="Menu 2" id="exper.menu_2" parent="exper.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="exper.menu_1_list" parent="exper.menu_1"
              action="exper.action_window"/>
    <menuitem name="Server to list" id="exper" parent="exper.menu_2"
              action="exper.action_server"/>
    -->
  </data>
</odoo>